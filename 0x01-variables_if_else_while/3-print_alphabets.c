#include <stdio.h>

/**
  * main - The entry point of the program
  *
  * Return: 0 successful execution
  */
int main(void)
{
	int n = 97;
	int m = 65;

	while (n <= 122)
	{
		putchar(n);
	}
	while (m <= 90)
	{
		putchar(m);
	}
	putchar('\n');
	return (0);
}
